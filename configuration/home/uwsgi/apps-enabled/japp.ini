[uwsgi]
base = /home/kollegen/myequis/japp
chdir = /home/kollegen/myequis

http = :8000
master = true
wsgi-file = %(base)/wsgi.py
touch-reload = %(wsgi-file)

static-map = /japp/static=%(chdir)/static

app = wsgi

virtualenv = %(chdir)/venv

plugin = python

uid = kollegen
gid = kollegen
